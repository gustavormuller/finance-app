# Autonomous run 005–010 — status

One line per checkpoint. Baseline before the run (commit 118fc1e): .NET 363 tests passed,
web unit 43 passed, E2E 13 passed.

## Checkpoint plans

- 005 checkpoints: CP1 domain + data model + migration (tests 1–4, 19; Transfer kind, rule 3, suggester, OpeningBalance, seeding) · CP2 Dapper aggregations + endpoints (tests 5–18) · CP3 web dashboard + Transfer in UI (tests 20–23) · CP4 E2E (24–26) · handoff.
- 006 checkpoints: CP1 data model + AddMarketData migration + upsert store + ADR-003/`prices` amendments (tests 17, 18, database half of 19, declared-type half of 9) · CP2 ports, provider registry, options with SGS codes and units, four provider adapters against fixtures (tests 1–9) · CP3 MarketDataSync, per-provider resilience, BackgroundService + Cronos (tests 10–16, 23, 24) · CP4 market-data endpoints + manual-sync rate limit (tests 19–22) · CP5 web `/market-data` + E2E + handoff (tests 25, 26).
- 007 checkpoints: CP1 data model + AddInvestments migration (context halves of tests 16, 23, 24; declared-type half of 9) · CP2 domain `PositionCalculator`, movement rules, `SnapshotBuilder` (tests 1–15) · CP3 `RebuildSnapshots` in one transaction, `/api/investments` endpoints, `POST /rebuild`, summary, nightly rebuild after the sync under the gate (tests 16–27, HTTP halves of 16, 23, 24) · CP4 web `/investments`, asset detail, movement form, add asset (tests 28–30) · CP5 E2E + handoff (tests 31–33).
- 008 checkpoints: CP1 decimal maths (`Exp`/`Ln`/`Pow`, no `double`), value objects, `BenchmarkAccumulator`, `Returns:Benchmarks` checked against 006's units at boot (tests 17–21; DoD "no `double` in `Domain/Returns/`" and "types match 006's units") · CP2 `TimeWeightedReturn`, `FxDecomposition`, portfolio aggregation (tests 1–8, 22–26) · CP3 `MoneyWeightedReturn` (Newton + bisection) and the timing effect (tests 9–16) · CP4 `ReturnsQueries` + `/api/returns/portfolio` and `/assets/{id}`: flows from `Movements` at 007's FX rule, periods and clamping, weekly sampling, missing benchmarks (tests 27–32) · CP5 web `/investments/returns` (tests 33–35) · CP6 E2E + handoff (test 36; the fakes need a shape).

## Log

| Spec | Checkpoint | Commit | Result |
|---|---|---|---|
| 004 | handoff | bec084f | OK — 004 complete in code (tests 1–66 present); manual steps 1–8 pending human |
| 005 | CP1 domain + migration | f6878c6 | OK — .NET 372, web 43, E2E 13; 3 TransactionPersistenceTests flaked once (DEFERRED) |
| 005 | CP2 Dapper aggregations + endpoints | ff3722b | OK — .NET 397, web 43, E2E 13; CP1 flake root-caused (inotify, config reload disabled in test hosts) |
| 005 | CP3 web dashboard + Transfer/openingBalance in UI | 1ae3b22 | OK — .NET 397, web 67, E2E 13 |
| 005 | CP4 E2E + handoff | 7f4468d | OK — .NET 397, web 67, E2E 16; 005 complete in code, manual steps 1–5 pending human |
| 006 | CP1 data model + migration | 0ed7f0c | OK — .NET 410, web 67, E2E 16. Paused before CP2: spec's two ports vs ADR-015 needs a human decision |
| 006 | CP2 ports, registry, settings, four providers | 542089d | OK — .NET 486, web 67, E2E 16. ADR-015 amended; all fixtures hand-written (network blocked) and SGS codes unverified, both pending human (DEFERRED) |
| 006 | CP3 sync, per-provider resilience, nightly job | d86fa0b | OK — .NET 521, web 67, E2E 16. Sync tests 10–14 run against PostgreSQL, not as unit tests; job off in test hosts and E2E via `MarketData:ScheduledSync`; container TZ pending human (DEFERRED) |
| 006 | CP4 market-data endpoints, manual sync + rate limit | 3cc56e5 | OK — .NET 535, web 67, E2E 16. Limit counts the latest run of any trigger (DB); one process-wide gate shared with the nightly job; run after the 202 in the background |
| 006 | CP5 web `/market-data` + E2E | eb6847f | OK — .NET 539, web 74, E2E 18. E2E API runs on `MarketData:FakeProviders` (Development only, boot refused elsewhere), which also zeroes the manual-sync window so E2E reruns never hit the 429 |
| 006 | handoff | c71a180 | OK — 006 complete in code (tests 1–26 present); provider keys, live fixtures, SGS codes, container TZ and manual steps 1–7 pending human |
| 007 | CP1 data model + migration | 0044305 | OK — .NET 549, web 74, E2E 18. No ADR conflict; ARCHITECTURE.md's Investments data-model block is stale against the spec (not edited, pending human) |
| 007 | CP2 domain PositionCalculator, MovementRules, SnapshotBuilder | 2248ce2 | OK — .NET 580, web 74, E2E 18. Full precision in the calculator, rounded once at the column (avg 8 places, BRL 2, half to even); broker agreement to the cent pending human (manual step 2). Invented pt-BR copy for fees ≥ 0 pending review (DEFERRED) |
| 007 | CP3a rebuild in one transaction, asset and movement routes | 12ced45 | OK — .NET 603 (web and E2E not rerun at 3a; no web change). Movement write and its rebuild share one transaction; DELETE uncovering a later sell is a 409 (spec silent, pending human) |
| 007 | CP3b positions, summary, `POST /rebuild`, rebuild after the sync | 9b30c32 | OK — .NET 611, web 76, E2E 18. USD realised/dividends at each event's own FX, rebuild also after manual syncs and from before yesterday where rows are missing (all spec-silent, pending human, DEFERRED); invented pt-BR copy pending review |
| 007 | CP4 web `/investments`, asset detail, movement form, add asset | f240856 | OK — .NET 611, web 115, E2E 18. Zero-quantity positions hidden by default so E2E 33 holds; stale = more than 3 weekdays, holidays unknown; Split form shows quantity only; float64 on the wire past 15 digits (all spec-silent, pending human, DEFERRED); invented pt-BR copy pending review |
| 007 | CP5 E2E 31–33 | afec18a | OK — .NET 611, web 115, E2E 21; two back-to-back `verify-e2e.sh` runs green. The investments spec runs in its own Playwright project after `market-data.spec.ts`, so its syncs never race test 26's 202 (test 26 unchanged); its own tests run in order and wait out a 429 |
| 007 | handoff | 74b569b | OK — 007 complete in code (tests 1–33 present); manual steps 1–6 against a broker (step 2 settles rounding), 006's provider keys, migration sign-off, spec-silent decisions, pt-BR copy review and the ARCHITECTURE/CLAUDE.md edits pending human |
| 008 | CP1 decimal maths, benchmark accumulator, `Returns:Benchmarks` at boot | 62a9c8a | OK — .NET 667, web 115, E2E 21. No ADR conflict, no migration. Spec test 15 (and 16) cannot pass as written, because a 4-day XIRR root is outside the bracket and the `decimal` range; CP3 respaces the dates unless told otherwise (pending human). IPCA+6 compounds whole months, with no pro-rata; `annualised` for periods of a year or less, and where labels live, are open (DEFERRED) |
| 008 | CP2 TWR, FX split, portfolio aggregation | 4473b99 | OK — .NET 701, web 115, E2E 21. No ADR conflict, no migration, domain only. Annualised TWR computed for any period, with `null` past the `decimal` range (spec silent; display is CP5's, pending human). Early flows land on the first row; USD flows at their own date's rate. Tests 15/16 still pending human: CP3 defaults to option (a), the same moves spread over a year, flagged (DEFERRED) |
| 008 | CP3 XIRR (Newton + bisection), XIRR flows, timing effect | efed343 | OK — .NET 730, web 115, E2E 21. No ADR conflict, no migration, domain only. NPV scaled so it never overflows (40-year tests). Tests 15/16 use option (a), the moves respaced over a year, with the literal four-day scenarios pinned as `null`: pending human sign-off, spec not edited. Test 13's "large late inflow" set is my reading; XIRR values checked against LibreOffice Calc. CP4 returns benchmark codes, labels in `labels.ts` (DEFERRED) |
| 008 | CP4 `/api/returns/portfolio` and `/assets/{id}`: periods, clamping, sampling, benchmarks | 19f794f | OK — .NET 759, web 115, E2E 21. No ADR conflict, no migration. One base day for TWR, benchmarks and chart (the first contribution's day at inception); XIRR opens on `from - 1`. Benchmark codes on the wire, `Label` config left unread; ordered by code. Sampling stride `ceil((n-1)/259)` days, weekly to 4.97 years. Assets with no row in the period left out; empty state is a 200 of nulls. Annualised TWR for ≤ 365 days not shown by CP5's default, invented pt-BR copy, and the 260-vs-weekly cap all pending human (DEFERRED) |
| 008 | CP5 web `/investments/returns` and `/investments/{id}/returns` | bd8c073 | OK — .NET 759, web 142, E2E 21. No ADR conflict, no API change. Annualised TWR hidden for ≤ 365 days (CP4 default); XIRR and timing effect "a.a."; the timing effect's loss colour is `--chart-expense` (orange; the app has no expense red); benchmark difference exact in p.p. on the period's totals; asset returns path invented; one call per asset; invented pt-BR copy. All pending human (DEFERRED) |
| 008 | CP6 E2E 36, shaped fakes, UI fixes | 132aa97 | OK — .NET 765, web 145, E2E 22; two back-to-back `verify-e2e.sh` runs green. Fake closes a cent lower per day before `to` (10 on `to`, 5 from 500 days back), so 006/007's E2E hold; test 36 on a new ticker per run, in a `returns` project after `investments`. First real-browser run found "1 dias" and repeated Y-axis ticks, both fixed test-first |
| 008 | handoff | f43811c | OK — 008 complete in code (tests 1–36 present); manual steps 1–5 against a real portfolio (step 3 settles XIRR, step 4 CDI), tests 15/16 option (a) sign-off, spec-silent decisions and pt-BR copy review pending human |
