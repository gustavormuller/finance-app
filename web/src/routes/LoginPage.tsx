/**
 * Not implemented yet: the sign-in link only. Reads no search parameters.
 */
export default function LoginPage() {
  return (
    <section>
      <h2>Sign in</h2>
      <a href="/api/auth/google">Sign in with Google</a>
    </section>
  );
}
